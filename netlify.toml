[build]
  command = "npx expo export -p web --output-dir dist/web"
  publish = "dist/web"

[functions]
  directory = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production]
  command = "npx expo export -p web --output-dir dist/web"
  publish = "dist/web"
